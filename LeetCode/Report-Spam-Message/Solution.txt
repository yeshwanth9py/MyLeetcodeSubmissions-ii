1class Solution {
2public:
3    bool reportSpam(vector<string>& message, vector<string>& bannedWords) {
4        unordered_set<string>st;
5        for(string str:bannedWords)st.insert(str);
6        int ct=0;
7        for(string str:message){
8            if(st.find(str)!=st.end())ct++;
9            if(ct==2)return true;
10        }
11        return false;
12    }
13};